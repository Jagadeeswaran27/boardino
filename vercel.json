{
  "build": {
    "env": {
      "VERCEL_ENV": "production"
    }
  },
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then echo 'Skipping build for non-main branch'; exit 0; else exit 1; fi"
}
